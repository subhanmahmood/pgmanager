# Binaries
pgmanager
*.exe

# Environment files
.env

# Data
data/
*.db

# IDE
.idea/
.vscode/
*.swp

# upgrade.sh --tag leaves a compose backup behind
examples/deploy/docker-compose.yml.bak

# Admin UI. Source lives in web/src; web/dist is the build output and IS
# committed, so `go run` serves a UI without a Node toolchain.
web/node_modules/
web/.vite/
web/node_modules/.tmp/
